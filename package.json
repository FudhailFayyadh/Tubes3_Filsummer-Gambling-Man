{
  "name": "judol-detector",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "vite build && vite build --mode content && vite build --mode ocr",
    "dev": "vite"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "@types/node": "^25.9.1",
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  },
  "dependencies": {
    "tesseract.js": "^7.0.0"
  }
}
